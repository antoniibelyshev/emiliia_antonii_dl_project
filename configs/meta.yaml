meta:
  wandb_ssh_key: "YOUR KEY"

  wandb_init:
    project: "test_run"
    entity: "emiliia_antonii_dl_project"

  type: "simultanious"
  # single -- single experiment
  # simultanious -- iterate over hyperparameters simultaniously
  # all -- consider all combinations of the hyperparameters

  use_gpu: True
  # if False then cpu is used
